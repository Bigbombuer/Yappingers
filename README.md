# Yap Engine X (Auto Thread + Keyword Filter)

Web app (Flask) untuk:
- ambil tweet terbaru akun project X
- filter keyword otomatis (optional)
- generate thread X: Clean + Degen
- tanpa Twitter API resmi

## Env / Secrets
Set di environment:
- GROQ_API_KEY
- X_AUTH_TOKEN
- X_CT0

## Install
```bash
pip install -r requirements.txt
playwright install chromium
